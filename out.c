
#include <stdio.h>
#include <stdlib.h>

/* change to 0 for no debug info to be printed: */
#define DO_SHOW 1
#define MEM_START 1

#include "cisc.h"

int main()
{

  START_MACHINE
  JUMP(CONTINUE)

  #include "char.lib"
  #include "io.lib"
  #include "math.lib"
  #include "scheme.lib"
  #include "string.lib"
  #include "system.lib"

CONTINUE:
PUSH(IMM(22))
CALL(MALLOC)
DROP(1)
MOV(ADDR(MEM_START), IMM(T_VOID))
MOV(ADDR(MEM_START + 1), IMM(T_NIL))
MOV(ADDR(MEM_START + 2), IMM(T_BOOL))
MOV(ADDR(MEM_START + 3), IMM(0))
MOV(ADDR(MEM_START + 4), IMM(T_BOOL))
MOV(ADDR(MEM_START + 5), IMM(1))
MOV(ADDR(MEM_START + 6), IMM(T_INTEGER))
MOV(ADDR(MEM_START + 7), IMM(1))
MOV(ADDR(MEM_START + 8), IMM(T_INTEGER))
MOV(ADDR(MEM_START + 9), IMM(2))
MOV(ADDR(MEM_START + 10), IMM(T_FRACTION))
MOV(ADDR(MEM_START + 11), IMM(1))
MOV(ADDR(MEM_START + 12), IMM(2))
MOV(ADDR(MEM_START + 13), IMM(T_FRACTION))
MOV(ADDR(MEM_START + 14), IMM(3))
MOV(ADDR(MEM_START + 15), IMM(7))
MOV(ADDR(MEM_START + 16), IMM(T_CHAR))
MOV(ADDR(MEM_START + 17), IMM('k'))
MOV(ADDR(MEM_START + 18), IMM(T_CHAR))
MOV(ADDR(MEM_START + 19), IMM(' '))
MOV(ADDR(MEM_START + 20), IMM(T_CHAR))
MOV(ADDR(MEM_START + 21), IMM('?'))

	MOV(R0, IMM(MEM_START + 6))
	MOV(R0, IMM(MEM_START + 8))
	MOV(R0, IMM(MEM_START + 4))
	MOV(R0, IMM(MEM_START + 1))
	MOV(R0, IMM(MEM_START + 10))
	MOV(R0, IMM(MEM_START + 13))
	MOV(R0, IMM(MEM_START + 16))
	MOV(R0, IMM(MEM_START + 18))
	MOV(R0, IMM(MEM_START + 20))

  PUSH(R0)
  CALL(WRITE_SOB)
  DROP(1)
  PUSH(IMM('\n'))
  CALL(PUTCHAR)
  DROP(1)
  STOP_MACHINE

  return 0;
}